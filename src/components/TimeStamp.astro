---
interface Props {
    startDate: string;
    endDate?: string;
}

const { startDate, endDate } = Astro.props;
---

<div class="flex flex-col items-center gap-1 py-2">
    <time>{endDate ?? "Actual"}</time>
    <span class="flex-grow rounded-sm w-1"></span>
    <time>{startDate}</time>
</div>

<style>
    span {
        background-color: var(--color-foreground-7);
    }
</style>
