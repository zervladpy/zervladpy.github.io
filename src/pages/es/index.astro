---
import { sections } from "@lib/section";
import { i18n } from "@lib/i18n";

const { es } = i18n;

import Layout from "@layouts/Layout.astro";

import Header from "@components/section/Header.astro";
import Hero from "@components/section/Hero.astro";
import Experience from "@components/section/Experience.astro";
import Education from "@components/section/Education.astro";
import Projects from "@components/section/Projects.astro";
import Skills from "@components/section/Skills.astro";
import Footer from "@components/section/Footer.astro";
import Section from "@components/section/Section.astro";

import {
    hero,
    social,
    experiences,
    educations,
    projects,
    about,
    skills,
} from "cv_es.json";
import About from "@components/section/About.astro";
---

<Layout
    title="Vladislav Zernov - Software Engineer"
    description={hero.description}
    locale="es"
>
    <Header
        links={[
            { label: es.nav.me, href: `#${sections.hero.id}` },
            {
                label: es.nav.experience,
                href: `#${sections.experience.id}`,
            },
            { label: es.nav.education, href: `#${sections.education.id}` },
            { label: es.nav.projects, href: `#${sections.projects.id}` },
            { label: es.nav.skills, href: `#${sections.skills.id}` },
            { label: es.nav.about, href: `#${sections.about.id}` },
        ]}
    />
    <Section id={sections.hero.id} class="mt-5">
        <Hero content={hero} social={social} />
    </Section>
    <Section id={sections.experience.id} title={es.nav.experience}>
        <Experience experiences={experiences} />
    </Section>
    <Section id={sections.education.id} title={es.nav.education}>
        <Education educations={educations} />
    </Section>
    <Section id={sections.projects.id} title={es.nav.projects}>
        <Projects projects={projects} />
    </Section>
    <Section id={sections.skills.id} title={es.nav.skills}>
        <Skills skills={skills} />
    </Section>
    <Section id={sections.about.id} title={es.nav.about}>
        <About about={about} />
    </Section>
    <Footer social={social} mail={hero.mail} />
</Layout>
